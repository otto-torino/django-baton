# Third-party runtime dependencies of the demo/test project (testapp).
# Used both by CI (see .github/workflows/tests.yml) and to run the app
# locally (see the "Development" section in the README).
#
# NOT listed here on purpose:
# - Django: provided by the CI matrix / your local environment, so its version
#   can vary independently.
# - django-baton: it is the library under test, installed from the repo root
#   with `pip install -e .`.
# - pillow: pulled in transitively by easy-thumbnails.
# - playwright: only needed for the e2e suite, see requirements-e2e.txt.
packaging
python-dotenv
django-tinymce
dj-editor-js==0.2.0
django-admin-rangefilter
django-admin-autocomplete-filter==0.6.1
django-import-export
django-select2
docutils
easy_thumbnails
django-mptt
django-modeltranslation
reportlab
requests
svglib
